%table{:cellspacing => "0"}
  - for row in infos
    %tr
      - for info in row
        %td
          = "#{info[0]}: "
          &nbsp;
        %td{:align => "right"}
          = info[2] == 'h' ? format_hour(info[1]) : f(info[1])
          = info[2]
        %td &nbsp; &nbsp; &nbsp;
%br/
