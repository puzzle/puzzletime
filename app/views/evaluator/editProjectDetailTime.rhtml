<h1>Editing worktime</h1>

<% if params.has_key?(:employee_id)  %>
  <%= start_form_tag :action => 'updateProjectDetailTime', :worktime_id => @worktime, :project_id => @project, :employee_id => @employee %>
    <%= render :partial => 'form' %>
<%= end_form_tag %>
<% else %>
 <%= start_form_tag :action => 'updateProjectDetailTime', :worktime_id => @worktime, :project_id => @project %>
  <%= render :partial => 'form' %>
<%= end_form_tag %>
<% end %>
 <% if params.has_key?(:employee_id)  %>
 <%= link_to 'Back', :action => 'selectProjectDetailTime', :employee_id => @employee, :project_id => @project %> 
 <% else %>
 <%= link_to 'Back', :action => 'selectProjectDetailTime', :project_id => @project %>
 <% end %>