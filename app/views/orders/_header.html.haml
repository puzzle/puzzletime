.orders-header
  .row
    .col-xs-2
      = link_to(orders_path(returning: true)) do
        = picon('chevron-left')
        Auftragsliste

    .order-search.col-xs-10
      = render 'chooser'

  = render 'tabs'
