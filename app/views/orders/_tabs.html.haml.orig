%ul.nav.nav-tabs
<<<<<<< Updated upstream
  = nav 'Details', order_path(@order)
  = nav 'Cockpit', cockpit_order_path(@order) if can?(:cockpit, @order)
  = nav 'Leistungen', services_order_path(@order) if can?(:services, @order)
  = nav 'Ziele', order_order_targets_path(@order) if can?(:targets, @order)
  //= nav 'Vertrag', ''
  = nav 'Journal', order_order_comments_path(@order) if can?(:comments, @order)
  //= nav 'Rechnungen', bills_order_path(entry)
=======
  = nav 'Details', order_path(entry)
  = nav 'Cockpit', cockpit_order_path(entry)
  = nav 'Leistung', '' #time_corrections_order_path(entry)
  = nav 'Ziele', targets_order_path(entry)
  = nav 'Vertrag', ''
  = nav 'Journal', order_order_comments_path(entry)
  = nav 'Rechnungen', bills_order_path(entry)
>>>>>>> Stashed changes
