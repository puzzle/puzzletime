.entry{'data-date' => day}
  .title= worktime_account(worktime)
  .time-range
    = time_range(worktime)
  .time-value= format_hour(worktime.hours)
  .controls
    = link_to(new_polymorphic_url(worktime, template: worktime),
              {title: 'Kopieren'}) do
      %i.icon-duplicate
    = link_to({:controller => worktime.controller,
              :action =>'destroy',
              :id => worktime.id},
              title: 'Löschen',
              data: { method: :delete,
                      confirm: ti(:confirm_delete) }) do
      %i.icon-delete
  .description= worktime_description(worktime)
  = link_to '', edit_polymorphic_url(worktime), {class: 'entry-link'}
