- daily_worktimes = daily_worktimes(@worktimes, day)
.date-label{'data-date' => day}
  .inner
    = format_day(day)
= render partial: 'worktime', collection: daily_worktimes, as: :worktime, locals: {day: day}
