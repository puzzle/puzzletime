- @title ||= ti(:title, model: full_entry_label).html_safe
- receipt = entry.receipt

.row.columns
  .col-12.col-md-6
    = render 'attrs'

    %br/
    = render 'backlink'
    = render 'actions_show'

  .col-12.col-md-6
    - if receipt.attached?
      = attachment_image_tag receipt, class: 'img-responsive'
