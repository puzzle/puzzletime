#menu.navbar.navbar-inverse
  .container-fluid
    %ul.nav.navbar-nav
      = nav 'Zeiterfassung',
            root_path,
            worktimes_path,
            ordertimes_path,
            absencetimes_path,
            employee_weekly_graph_path(current_user.id)
      = model_nav Order
      = nav 'Planung',
            plannings_employee_path(current_user.id),
            plannings_orders_path,
            plannings_employees_path,
            plannings_departments_path,
            plannings_company_path,
            plannings_custom_lists_path
      = nav 'Auswertungen',
            evaluation_path('userworkitems', clear: 1),
            '/evaluator',
            reports_orders_path,
            reports_workload_path,
            reports_revenue_path,
            vacations_path
      - if can?(:manage, Client)
        = nav 'Verwalten',
              clients_path,
              clients_path,
              absences_path,
              departments_path,
              employees_path,
              user_notifications_path,
              holidays_path,
              order_kinds_path,
              order_statuses_path,
              portfolio_items_path,
              target_scopes_path,
              working_conditions_path
