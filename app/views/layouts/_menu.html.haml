#menu.navbar.navbar-inverse
  .container-fluid
    %ul.nav.navbar-nav
      = nav 'Zeiterfassung',
            worktimes_path(clear: 1),
            worktimes_path,
            ordertimes_path,
            absencetimes_path,
            weekly_graph_path
      = nav 'Planung', plannings_path(clear: 1), plannings_path, employee_lists_path
      = nav 'Auswertungen',
            evaluation_path('userworkitems', clear: 1),
            '/evaluator',
            all_absences_graph_path
      = nav 'Verwalten',
            orders_path,
            clients_path,
            absences_path,
            departments_path,
            employees_path,
            user_notifications_path,
            holidays_path,
            order_kinds_path,
            order_statuses_path,
            portfolio_items_path,
            target_scopes_path

