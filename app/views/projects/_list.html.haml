- @title = projects_title

= crud_table :name, :description do |t|
  - t.action_col { |p| link_to('Subprojekte', sub_projects_path(p)) if p.sub_projects? }
  - t.action_col { |p| link_to('Mitarbeiter', project_memberships_path(p)) }
