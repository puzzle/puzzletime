= javascript_include_tag :defaults, :cache => true
:javascript
  //
  function updatePlanning() {
  	employee_id = $('planning_employee_id').value;
  	with_params = 'employee_id=' + employee_id;
  	if ($('planning_start_week_date')) {
  		with_params = with_params + '&start_week_date=' + $('planning_start_week_date').value;
  	}
  	#{remote_function(:url => { :action => 'existing' }, 
  						:method => :get,
  						:with => "with_params")}
  }
  //
