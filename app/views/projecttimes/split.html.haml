- @title = @split.page_title
%h3
  = @split.original.time_string
  am #{f(@split.original.work_date)}:
%table.table-striped{:cellpadding => "0"}
  %tbody
    = render :partial => 'part', :collection => @split.worktimes
%p Die Zeiten wurden noch nicht gespeichert. Bitte schliessen sie dazu den Aufteilungsprozess ab.

= form_tag(evaluation_detail_params.merge!({:controller => @worktime.controller, :action => 'create_part', :id => @worktime})) do
  = render(:partial => 'form', :locals => {:submit => @split.class::SUBMIT_BUTTONS })

- options = evaluation_detail_params
- if params[:evaluation]
  - options[:controller] = 'evaluator'
  - options[:action] = 'details'
- else
  - options[:action] = 'index'

- htmlOptions = {}
- unless @split.empty?
  - htmlOptions[:onclick] = "return confirm('Alle Änderungen an dieser Aufteilung gehen dabei verloren!');"

%p= link_to 'Arbeitszeiten', options, htmlOptions
