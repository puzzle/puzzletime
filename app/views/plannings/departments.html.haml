- @title = "Bereiche"

= DryCrud::Table::Builder.table(@departments,
                                self,
                                class: 'table-condensed table-striped table-hover') do |t|
  - t.col('Bereich') { |d| d.name }
  - t.action_col do |d|
    = link_to 'Mitarbeiterplanung',
               :action => 'department_planning', :department_id => d.id
