- @title = "Planung von #{@employee.label}"
= render :partial => 'plannings/period_link', :locals => {:id => @employee}

%div.table-responsive
  %table.absence
    %thead
      = render :partial => 'plannings/header'
    %tbody
      = render :partial => 'plannings/employee_planning_entry', :locals => {:user => @employee, :employee_planning_graph => @graph}

%br/
%br/
= render :partial => 'legend'
