= with_tooltip("#{format_number(@board.total_planned_hours, 0)}h (inkl. Abwesenheiten) von #{format_number(@board.total_plannable_hours, 0)}h (abz. Feiertage)") do
  = format_number(@board.total_planned_hours, 0)
  von
  = format_number(@board.total_plannable_hours, 0)
  Stunden verplant
