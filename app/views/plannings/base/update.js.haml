- @board.rows.each do |(employee_id, work_item_id), items|
  $('#planning_row_employee_#{employee_id}_work_item_#{work_item_id}').html('#{j(render(partial: 'row', locals: { items: items, employee: items.compact.first }))}');
  App.plannings.panel.close();
