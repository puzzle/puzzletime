- @title = 'Wochengrafik'
= stylesheet_link_tag 'graph', :media => 'all'
= render(:partial => 'evaluator/period_link')
%table.graph
  %tr.head
    %th
    = weekday_header
    - @graph.each_day do |day|
      - if day.wday == 1
        = "</tr><tr>".html_safe
        %th
          KW #{day.cweek} &nbsp;
      = day_td day do
        - @graph.timeboxes.reverse_each do |box|
          = timebox_div box
        .dateLabel= I18n.l(day)



%table.legend
  = render :partial => "legend", :collection => [Project, Absence]
  %tr
    %td{:colspan => "2"}
      %b Weiteres
  = render( :partial => 'legend_item', :collection => [['Wochenende / Feiertag', '#DDDDDD' ]] )
  %tr
    %td= timebox_div Timebox.new(nil, '#FF0000', 1, '')
    %td
      Tagessoll (#{"%0.2f" % (Settings.must_hours_per_day * @graph.must_hours_factor)}
      h)
%br{:clear => "all"}
