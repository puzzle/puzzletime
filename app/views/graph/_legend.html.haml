- if @graph.accounts? legend
  %tr
    %td{:colspan => "2"}
      %b= legend.label_plural
  - @graph.accounts_legend(legend).each do |l|
    = render :partial => 'legendItem', :locals => {:legendItem => l}
